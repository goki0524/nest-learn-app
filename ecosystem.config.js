module.exports = {
  apps: [
    {
      // name: `${process.env.npm_package_name}-v${process.env.npm_package_version}`,
      // script: 'dist/main.js',
      // listen_timeout: 10000, // 10 seconds
      // error_file: 'logs/err.log',
      // out_file: 'logs/out.log',
      // log_file: 'logs/combined.log',
      // log_date_format: 'YYYY-MM-DD HH:mm:ss:SSS',
      // min_uptime: 10000, // 10 seconds
      // max_restarts: 3,
      // max_memory_restart: '2G', // will restart if memory uses 2GB
      // // use node at nvm
      // interpreter: `node@${process.env.npm_package_engines_node}`,
    },
  ],
}
